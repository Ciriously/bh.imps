import React from "react";
import "./Header.css"; // Import the CSS file for the Header component

function Header() {
  return (
    <div className="header">
      <h1 className="title">Brainheaters IMPs</h1>
      <p className="text">
        These are some of the list of IMP Questions, topics, modules from
        BH.Qbanks & Notes
      </p>
    </div>
  );
}

export default Header;
